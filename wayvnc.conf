# wayvnc is bound to the LAN address by the launcher, not to 0.0.0.0.
# RFB carries no transport encryption in this configuration, so this is a
# trusted-LAN setup only -- do not port-forward 5900.
use_relative_paths=true
